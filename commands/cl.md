---
description: cl cheat sheet — all switch/session commands (incl. zero-token cl:switch)
allowed-tools: Bash(node:*)
---

!`node "$HOME/.claude/scripts/cl-help.js"`

Above is the cl command cheat sheet. Present it to the user verbatim (a code
block is fine). Do not take any further action.

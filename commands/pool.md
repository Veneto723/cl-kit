---
description: Show pool account status — 5h/7d utilization and reset times
allowed-tools: Bash(node:*)
---

!`node "$HOME/.claude/scripts/pool-status.js"`

The above is the current pool account status (from the configured pool DB).
Just present it to the user as-is — do not take any further action.

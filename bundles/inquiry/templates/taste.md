# Taste profile — <user>

<!-- The inquiry reads this every run to RANK findings and decide WHEN to
     interrupt you. Non-negotiable rules baked into the skill:
       • Taste touches RANKING + ESCALATION only — NEVER generation/divergence.
         (A loop that only confirms your taste is an echo chamber.)
       • It encodes taste-in-QUALITY (evidence, feasibility, novelty, repos),
         NEVER taste-in-CONCLUSIONS (what answer you want to hear).
       • Prior, not law: it nudges order + the escalation call; it never overrides
         the skeptic, the sources, or the on-brief gate.
       • Curated, not append-only: distilled every few runs, stale signals decayed.
     Lives at the resolved inquiry home so skill redeploys never wipe it:
     INQUIRY_HOME, then an existing ~/.inquiry, then an existing ~/.claude/inquiry,
     otherwise ~/.inquiry. -->

## Hard constraints (gates — a finding that violates one is not for me)
<!-- e.g. must be on-device feasible · commercially license-clean · no vaporware/synthetic preprints -->

## What makes a finding VALUABLE to me (quality rubric — nudges ranking, breaks ties)
<!-- e.g. assumption-inverting > incremental · a working repo is a plus · falsifiable > vibes ·
     cross-domain transplant is a plus · pure benchmark-methodology is low-value -->

## Escalate-worthy? — labeled examples (the loop few-shots on THESE, not on a bare number)
### YES — worth interrupting me for
<!-- - <one-line finding> — why: changed my strategy / broke a core assumption -->
### NO — noise, don't interrupt
<!-- - <one-line finding> — why: incremental / obvious / off my direction -->

## Standing interests / recurring limiters (my context, so the loop doesn't re-derive it)
<!-- the ceilings I keep circling -->

## Changelog (distillation history)
<!-- <date> · distilled N feedback items: <what changed / decayed> -->
